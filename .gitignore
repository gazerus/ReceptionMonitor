node_modules/
dist/
.DS_Store
*.log

# Capacitor iOS project (not built for this device, generate with `npx cap add ios` if needed)
packages/reception/ios/

# Server secrets — never commit real credentials
server/secrets.php
server/config.json
